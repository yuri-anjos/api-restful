spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    open-in-view: false

server:
  port: 8888

spring-doc:
  pathsToMatch: /**/v1/**
  swagger-ui:
    use-root-path: true
